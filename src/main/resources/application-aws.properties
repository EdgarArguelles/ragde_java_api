server.port=5000
server.servlet.context-path=/
#DataLoader (if true loads default values when Data Base is empty when application starts)
data-loader=false
#spring.jpa.show-sql=true
############GraphQL############
graphql.spqr.gui.target-endpoint=/graphql
#AWS
spring.datasource.url=jdbc:mysql://${RDS_HOSTNAME}:${RDS_PORT}/ragde?useSSL=false&serverTimezone=UTC
spring.datasource.username=${RDS_USERNAME}
spring.datasource.password=${RDS_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver